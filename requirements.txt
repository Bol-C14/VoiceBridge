# Editable installs require a recent pip (PEP 660). If this fails, run:
#   python -m pip install -U pip

-e packages/voicebridge_core
-e apps/daemon
-e apps/cli

# Optional (uncomment as needed):
# -e "packages/voicebridge_core[audio,vad,asr]"
